name: flutter_themez
description: A simple wrapper for Flutter's ThemeData. Including "light" and "dark".
version: 1.2.1
repository: https://github.com/mj12358/flutter-theme

environment:
  sdk: ">=2.18.2 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  dart_lintz: ^1.1.0
  flutter_test:
    sdk: flutter
