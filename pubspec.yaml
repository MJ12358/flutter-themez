name: flutter_themez
description: A simple wrapper for Flutter's ThemeData. Including "light" and "dark".
version: 1.0.6
repository: https://github.com/mj12358/flutter-theme

environment:
  sdk: ">=2.18.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_extensionz: ^2.0.1

dev_dependencies:
  dart_lintz: ^1.0.0
  flutter_test:
    sdk: flutter
